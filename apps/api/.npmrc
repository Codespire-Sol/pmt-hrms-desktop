# Cap parallel downloads so a rate-limited registry/proxy doesn't return 503s.
network-concurrency=4

# Retry count and timeouts for resilience against transient network errors.
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=300000
